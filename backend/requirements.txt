# SunChat Backend Dependencies

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.19

# HTTP
requests>=2.31.0

# Database (同步 SQLite)
sqlalchemy==2.0.36

# Vector Database
chromadb==0.5.5

# Search (ddgs 为主，duckduckgo-search 作旧包回退)
ddgs>=9.0.0
duckduckgo-search==5.0.0

# Config & Validation
python-dotenv==1.0.1
pydantic==2.10.0
pydantic-settings==2.7.0

# PDF Processing (for knowledge base)
pypdf==5.1.0

# ASR 语音转文字 (R-009, 本地 faster-whisper, av 免系统 ffmpeg)
faster-whisper>=1.0.0

# 姿态估计 (R-017, MediaPipe Pose tasks API;锁版=与 numpy1.26.4/chromadb 共存已实测,
# mediapipe>=1.0 强制 numpy2 会破坏 chromadb 约束——禁止自动升级)
mediapipe==0.10.35
