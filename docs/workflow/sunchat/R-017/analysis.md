# R-017 变更分析(v2 重构依据)

分析需求:支撑"抽帧方案重做 + 配速条件性纳入"变更;协议见 code-analysis.md。
范围:backend/core/pose.py、routes/chat.py、config.py,真人实拍两次 + 合成实验四组。

## A-1 前段无效信息稀释步频(已确认)
问题:真人两次实测 cadence=48.6/54.4(物理不可能),数值域与死段时长负相关。
- [CODE] backend/core/pose.py:238 — `span = ts_a[det[-1]] - ts_a[det[0]]`;
  cadence = IC计数 ÷ 检出人物质心的总跨度(非跑动段时长)
- [CODE] pose.py analyze_video — det = "检出人体即入列",站立/走位/掏手机帧 conf 高
  → span 被无效段拉长,IC 计数不变 → 步频被稀释
- [RUN] 合成实验(会话 2026-09-13):纯跑3s cad=142 → 前3s站立+跑3s cad=80.7 →
  前6s站立+跑4s cad=66.3;cycles 几乎不变、span 翻倍,稀释机制实锤
- 结论 C-A1:现有抽帧与"内容是否有效"完全无关;真实手持拍摄必然含入画/走动/拿手机
  段,该缺陷稳定复现,与竖直法好坏正交。

## A-2 帧率元数据不可信,真实帧率只能逐帧 PTS 实测(已确认)
问题:变更需"检测真实帧率",先验证元数据可用性。
- [RUN] 合成 120帧/标称240fps/PTS拉长8×(模拟 iPhone 慢动作容器语义):
  `avg_rate=28800/953≈30.2` 与元数据打架;PyAV 无 container.time_base
- [RUN] 请求20fps采样:源30fps成片取40帧@19.8fps(正常);源真60fps 同样只取40帧
  (上限20fps 生效)——采样按**显示时间轴**,慢动作时间被拉长→等效常速过采 N 倍,
  但 cadence=事件/显示秒 → 慢动作素材测得步频=真实值÷N(48.6 若源自慢动作也是这条通道)
- 结论 C-A2:必须 demux 全量 packet PTS → 中位间隔倒数 = 实测显示帧率 f_eff;
  标称fps(NOMINAL,如 iPhone 慢动作 240)与 f_eff 之比 ∈{2,4,8} 即慢动作倍速 N,
  cadence 还原 = cad×N(VFR 抖动>0.35 标记降级)。

## A-3 配速单目可行:自标定实验(已确认,±20% 级)
问题:纯关键点能否还原配速(用户确认"能算就算")。
- [RUN] 原型 v5(干净几何夹具):视场960px=12m,真值 V=2.6m/s(配速6:25)、cad160、
  身高175cm(腿长0.9275m):
  ①尺度:腿长=|髋-膝|+|膝-踝| 姿态无关刚性,身高×0.53→米/帧标定,scale 还原 83.3px/m
    (真值80,+4%);
  ②步长:固定机位下 stance 期踝世界坐标钉地("落地事件间跑者前进=步长"),
    相邻落地点像素距×scale = 0.936m(真0.975,-4%);
  ③端到端 v=步长×cad/60 = 2.10m/s(误差19%,主导项是夹具中 cad 低估16%,与步长无关)
- [DESIGN] 用户会话确认:"能算就算";跑步机场景无画面位移不可解(读数即真值);
  跟随机位不可解(横移为0)
- 结论 C-A3:固定机位侧拍 + 身高已知 → 配速可达 ±20~25%(≈±1.5min/km);
  算不出(跟随机位/跑步机/未配身高)必须显式 null+原因,不得给假数字。

## A-4 竖直采样频率账(设计输入)
- [RUN] cad 180 → 髋2f=6Hz、踝1f=3Hz、stance 0.13s;段内密采 25fps=每步6.6帧/
  stance 3.2帧,事件级指标可用;400帧上限对 30fps≤16s 视频不构成瓶颈,对长视频才是
- ⚠假设(低危):粗扫5fps 检测"跑动存在"够用(髋y 主峰≤3Hz<奈奎斯特2.5…临界)。
  验证方法:实现后以 5fps 跑夹具断言不漏检 → 若漏则粗扫提至 8fps(代价 +2s)。
